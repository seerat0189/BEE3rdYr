/* 08-07-2025
callbacks and promises (to handle asynchronous programs)
promise is an object which represents eventual completion or failure of an asynchronous operation
states in promise are pending, fulfilled and rejected
if state is fulfilled, .then works
if state is rejected, .catch works
inversion control
*/